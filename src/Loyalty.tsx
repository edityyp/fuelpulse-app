import {useState} from 'react';import type {Catalog} from '../shared/contracts';import {api,errorText} from './api';
export function LoyaltyScreen({catalog,refresh,notify}:{catalog:Catalog;refresh:()=>Promise<void>;notify:(s:string)=>void}){const[data,setData]=useState<{balance:string;ledger:{transaction_id:string;points:number;created_at:string}[]}|null>(null),[error,setError]=useState('');return <div className="grid"><section className="panel"><h2>Customer points ledger</h2><form onSubmit={async e=>{e.preventDefault();const plate=String(new FormData(e.currentTarget).get('plate'));try{setData(await api('/points?plate='+encodeURIComponent(plate)));setError('');}catch(e){setError(errorText(e));}}}><label>Vehicle number<input name="plate" required/></label><button>Look up balance</button></form><p role="status">{error}</p>{data&&<><h2>{data.balance} points</h2>{data.ledger.map(l=><div className="row" key={l.transaction_id}><span>{new Date(l.created_at).toLocaleString()}</span><strong>+{l.points}</strong></div>)}</>}</section><section className="panel"><h2>Loyalty reward rule</h2><p className="muted">Managers and owners can decide how many points unlock a reward and what the customer receives.</p><form onSubmit={async e=>{e.preventDefault();const d=new FormData(e.currentTarget);try{await api('/reward-settings',{reward_threshold_points:Number(d.get('threshold')),reward_name:d.get('reward')});await refresh();notify('Loyalty reward updated');}catch(e){notify(errorText(e));}}}><label>Points required<input name="threshold" type="number" min="1" max="1000000" defaultValue={catalog.organization.reward_threshold_points} required/></label><label>Reward customer receives<input name="reward" minLength={2} maxLength={120} defaultValue={catalog.organization.reward_name} required/></label><button className="primary">Save loyalty reward</button></form><div className="info"><strong>Current rule</strong><p>{catalog.organization.reward_name} after every {catalog.organization.reward_threshold_points} points.</p></div></section></div>;}
