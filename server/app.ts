import Fastify from 'fastify';import cookie from '@fastify/cookie';import helmet from '@fastify/helmet';import files from '@fastify/static';import rateLimit from '@fastify/rate-limit';import {ZodError} from 'zod';import {resolve} from 'node:path';import {existsSync} from 'node:fs';import {authRoutes} from './auth.js';import {routes} from './routes.js';import {pool} from './db.js';
export async function buildApp(){const origin=process.env.APP_ORIGIN??'http://localhost:3000';if(process.env.NODE_ENV==='production'&&!origin.startsWith('https://'))throw Error('Production requires HTTPS');const app=Fastify({bodyLimit:16384,trustProxy:false,logger:{level:'warn',redact:['req.headers.cookie','req.headers.authorization','req.body','res.headers.set-cookie']},requestTimeout:30000});await app.register(cookie);await app.register(rateLimit,{max:300,timeWindow:'1 minute'});await app.register(helmet,{contentSecurityPolicy:{directives:{defaultSrc:["'self'"],scriptSrc:["'self'","'wasm-unsafe-eval'"],styleSrc:["'self'"],imgSrc:["'self'",'data:','blob:'],workerSrc:["'self'",'blob:'],connectSrc:["'self'"],objectSrc:["'none'"],frameAncestors:["'none'"],upgradeInsecureRequests:process.env.NODE_ENV==='production'?[]:null}}});app.addHook('onRequest',async(req,reply)=>{if(req.url.startsWith('/api/')){reply.header('Cache-Control','no-store');if(!['GET','HEAD','OPTIONS'].includes(req.method)&&req.headers.origin!==origin)return reply.code(403).send({error:'Invalid origin'});}});app.setErrorHandler((error,_req,reply)=>{const e=error as {statusCode?:number;code?:string;message?:string};const status=error instanceof ZodError?400:e.code==='23505'?409:['23503','42501','22P02'].includes(e.code??'')?400:e.statusCode??500;reply.code(status).send({error:status>=500?'Server error; retry safely':status===400?'Invalid request':status===409?'Conflict; request cannot be applied':e.message??'Request failed'});});await authRoutes(app);routes(app);app.get('/api/health',async()=>{await pool.query('select 1');return {status:'ok'};});if(existsSync(resolve('dist'))){await app.register(files,{root:resolve('dist')});app.setNotFoundHandler((req,reply)=>req.url.startsWith('/api/')?reply.code(404).send({error:'Not found'}):reply.sendFile('index.html'));}return app;}
